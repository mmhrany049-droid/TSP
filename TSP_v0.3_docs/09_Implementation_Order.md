# ترتیب صحیح پیاده‌سازی — TSP v0.3

این ترتیب را **دقیقاً** رعایت کن تا پروژه قابل اجرا و قابل تست مرحله‌ای بماند.

---

## فاز ۰: آماده‌سازی

1. خواندن کامل اسناد 01 تا 06 و 10
2. ساخت پوشه backend و frontend جدا

---

## فاز ۱: Backend پایه

1. پرامپت ۱ Backend → اسکلت + مدل‌ها
2. راه‌اندازی دیتابیس و Alembic
3. پرامپت ۲ Backend → احراز هویت
4. تست ثبت‌نام و ورود با ابزارهایی مثل Swagger یا curl

**معیار موفقیت فاز ۱:**  
می‌توان کاربر ساخت و توکن JWT گرفت.

---

## فاز ۲: Frontend پایه

1. پرامپت ۱ Frontend → اسکلت
2. پرامپت ۲ Frontend → صفحات ورود و ثبت‌نام + اتصال به Backend
3. تست کامل جریان ثبت‌نام → ورود → ورود به داشبورد خالی

**معیار موفقیت فاز ۲:**  
کاربر از طریق مرورگر ثبت‌نام و وارد می‌شود.

---

## فاز ۳: کتاب و تست

1. پرامپت ۳ Backend → LibraryManager
2. پرامپت ۴ Backend → QuestionBank + TopicTree
3. پرامپت ۳ Frontend → صفحات کتاب
4. پرامپت ۴ Frontend → صفحات تست و مبحث

**معیار موفقیت فاز ۳:**  
می‌توان کتاب ساخت، ساختار داد، تست وارد کرد و به مبحث وصل کرد.

---

## فاز ۴: تلاش و مرور

1. پرامپت ۵ Backend → AttemptTracker + SmartReview
2. پرامپت ۵ Frontend → صفحات حل تست و مرور

**معیار موفقیت فاز ۴:**  
می‌توان تست زد، نتیجه دید، تاریخچه حفظ شد و مرور کرد.

---

## فاز ۵: تدریس، آزمون، آمادگی، داشبورد

1. پرامپت ۶ Backend
2. پرامپت ۶ Frontend

**معیار موفقیت فاز ۵:**  
تمام قابلیت‌های اصلی قابل استفاده هستند.

---

## فاز ۶: نهایی‌سازی و پولیش

1. پرامپت ۷ Backend
2. پرامپت ۷ Frontend
3. تست سرتاسری
4. رفع باگ‌ها
5. نوشتن READMEهای نهایی

---

## نکته بسیار مهم

- هر فاز را تمام کن و تست کن، بعد به فاز بعدی برو.
- Backend را جلوتر از Frontend همان ماژول پیش ببر.
- اگر چیزی مبهم بود، به اسناد 01 تا 04 برگرد.
