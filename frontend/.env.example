# اگر این متغیر تنظیم شود، مرورگر مستقیم به بک‌اند وصل می‌شود.
# اگر خالی بماند، درخواست‌ها به /api/v1 می‌روند و Next آن‌ها را به پورت ۸۰۰۰ پروکسی می‌کند.
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# فقط برای پروکسی سمت سرور Next
API_PROXY_TARGET=http://127.0.0.1:8000
